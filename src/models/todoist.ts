import project from './project';
import task from './task';
import section from './section';

export class todoist {
    projects: project[] = [];
    tasks: task[] = [];
    sections: section[] = [];
}
